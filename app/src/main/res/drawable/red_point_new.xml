<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 实心 -->
    <solid android:color="@color/redPoint"/>
    <!-- 边框 -->
    <stroke
        android:width="0.5dp"
        android:color="@color/redPoint"/>
    <!-- 圆角 -->
    <corners android:radius="5dip"/>
    <!-- 边距 -->
    <padding
        android:bottom="4dip"
        android:left="8dip"
        android:right="8dip"
        android:top="4dip"/>
</shape>